# Interface
streamlit>=1.35

# Named entity recognition for answer extraction (Stage 3).
# The model is pinned by URL because Streamlit Community Cloud installs from
# this file and cannot run `python -m spacy download`.
spacy>=3.7,<4.0
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
